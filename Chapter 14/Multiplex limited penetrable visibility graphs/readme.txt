"main.m" is the main program;

"MVG.m" computes the limited penetrable visibility graphs weighted brain networks;

"CLPVG.m" computes the limited penetrable visibility graphs;

BCT toolbox computes the indicators of brain networks and selects thresholds;

"TR.m" computes the transitivity integral within the threshold range.
